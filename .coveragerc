# Coverage.py is a tool for measuring code coverage of Python programs.
# for more info: https://coverage.readthedocs.io/en/latest/config.html
[run]
omit =
    afwf_awscli_profile/tests/*
    afwf_awscli_profile/handlers/run_set_default_profile.py
    afwf_awscli_profile/handlers/run_mfa_auth.py
    afwf_awscli_profile/handlers/view_or_edit_settings.py
    afwf_awscli_profile/_version.py

[report]
# Regexes for lines to exclude from consideration
exclude_lines =
    # Have to re-enable the standard pragma
    pragma: no cover

    # Don't complain about missing debug-only code:
    def __repr__
    if self\.debug

    # Don't complain if tests don't hit defensive assertion code:
    raise AssertionError
    raise NotImplementedError

    # Don't complain if non-runnable code isn't run:
    if 0:
    if __name__ == .__main__.:
